#!/bin/bash

ver_rvm="$1"; shift

shome=$(pwd)
PATH="$shome/sbin:$PATH"

env HOME="$shome" rvm_path="$shome/.rvm" $shome/.rvm/bin/rvm-installer --version $ver_rvm --without-gems="rvm rubygems-bundler" --quiet-curl
